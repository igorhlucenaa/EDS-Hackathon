import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esportesdasorte.app',
  appName: 'Hackathon Esportes da Sorte',
  webDir: 'dist'
};

export default config;
